<div class="container mx-auto py-6 max-w-3xl">
  <h1 class="text-3xl font-bold text-gray-800 mb-6">Edit Topping</h1>

  <%= form_with model: @topping, local: true do |form| %>
    <%= render 'form', form: form, field: :name, label: 'Topping Name' %>
    <%= render 'form', form: form, field: :description, label: 'Description (Optional)' %>

    <div class="mt-6">
      <%= form.submit 'Update Topping', class: 'w-full py-2 px-4 bg-blue-500 text-white font-semibold rounded-lg shadow-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50' %>
    </div>
  <% end %>
</div>
